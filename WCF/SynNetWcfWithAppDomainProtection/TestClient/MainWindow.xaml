<Window x:Class="TestClient.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TestClient"
    Title="MainWindow" Height="500" Width="900" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <local:ViewModel />
    </Window.DataContext>
    <Grid>
        <DataGrid ItemsSource="{Binding Parts}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Part ID" Binding="{Binding Id}" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" />
                <DataGridTextColumn Header="Product Group" Binding="{Binding Groupid}" />
                <DataGridTextColumn Header="Supplier" Binding="{Binding Supplierid}" />
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Window>
